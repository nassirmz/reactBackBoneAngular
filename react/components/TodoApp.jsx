var React = require('react');
var ReactDOM = require('react-dom');

var TodoApp = React.createClass({
  render: function () {
    return (
      <p>Todo App rendered</p>
    );
  }
});

module.exports = TodoApp;
